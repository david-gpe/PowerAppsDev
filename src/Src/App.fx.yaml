App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =Set(
            usuario,
            {
                Nombre: "David",
                Correo: "demo@email.com"
            }
        )
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

